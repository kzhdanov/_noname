import React from 'react';
import Icon from 'react-native-vector-icons/SimpleLineIcons';

export const screenTypes = {
  failed: 'failed',
  active: 'active',
  done: 'done',
};

export const allIcon = <Icon name='docs' size={24} />;
export const doneIcon = <Icon name='check' size={24} />;
export const failedIcon = <Icon name='close' size={24} />;